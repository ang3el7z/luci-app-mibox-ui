name: Build luci-app-mibox-ui from TAG
run-name: Build release TAG ${{ github.ref_name }}

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  # Джоб для синка версии Makefile и фронта
  sync-version:
    name: Sync Makefile version to branch
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5.0.0
        with:
          fetch-depth: 0

      - name: Detect target branch from tag
        id: detect_branch
        run: |
          set -e
          git fetch origin "+refs/heads/*:refs/remotes/origin/*"
          BRANCH=""
          for b in $(git branch -r --contains "$GITHUB_SHA" | grep -v "origin/HEAD" | sed "s#origin/##"); do
            [ -z "$b" ] && continue
            if [ "$b" != "${DEFAULT_BRANCH}" ]; then
              BRANCH="$b"
              break
            fi
          done
          if [ -z "$BRANCH" ]; then
            BRANCH=$(git branch -r --contains "$GITHUB_SHA" | grep -v "origin/HEAD" | head -n 1 | sed "s#origin/##")
          fi
          if [ -z "$BRANCH" ]; then
            BRANCH="${DEFAULT_BRANCH}"
          fi
          echo "TARGET_BRANCH=$BRANCH" >> "$GITHUB_ENV"
          echo "📌 Will push Makefile version sync to branch: $BRANCH"
        env:
          DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}

      - name: Update Makefile version to tag and push to branch
        run: |
          set -e
          TAG_VERSION=${GITHUB_REF#refs/tags/v}

          # Makefile
          sed -i "s/^PKG_VERSION:=.*$/PKG_VERSION:=${TAG_VERSION}/" luci-app-mibox-ui/Makefile

          # JS фронт (если есть)
          DASHBOARD="luci-app-mibox-ui/htdocs/luci-static/resources/view/mibox-ui/dashboard.js"
          if [ -f "$DASHBOARD" ]; then
            sed -i "s/^const APP_VERSION = '.*';/const APP_VERSION = '${TAG_VERSION}';/" "$DASHBOARD"
          fi

          if git diff --quiet; then
            echo "ℹ No changes to commit."
          else
            if git show-ref --verify --quiet "refs/remotes/origin/${TARGET_BRANCH}"; then
              git add luci-app-mibox-ui/Makefile
              [ -f "$DASHBOARD" ] && git add "$DASHBOARD"
              git -c user.name="github-actions[bot]" -c user.email="github-actions[bot]@users.noreply.github.com" \
                commit -m "github-actions[bot]: sync version with tag v${TAG_VERSION}"
              git push origin HEAD:${TARGET_BRANCH}
            else
              echo "⚠️ Target branch '${TARGET_BRANCH}' not found. Skipping push."
            fi
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Джоб для сборки ipk и apk через Docker
  build:
    name: Build (${{ matrix.pkg_type }})
    runs-on: ubuntu-latest
    needs: sync-version
    strategy:
      fail-fast: false
      matrix:
        pkg_type: [ipk, apk]

    steps:
      - name: Checkout
        uses: actions/checkout@v5.0.0
        with:
          fetch-depth: 0

      - name: Set Makefile version to tag (for build only)
        run: |
          TAG_VERSION=${GITHUB_REF#refs/tags/v}
          sed -i "s/^PKG_VERSION:=.*$/PKG_VERSION:=${TAG_VERSION}/" luci-app-mibox-ui/Makefile
          echo "📦 Building with PKG_VERSION=$TAG_VERSION"

      - name: Build Docker image (${{ matrix.pkg_type }})
        uses: docker/build-push-action@v6.18.0
        with:
          context: .
          file: Dockerfile-${{ matrix.pkg_type }}
          tags: mibox-ui-${{ matrix.pkg_type }}:ci

      - name: Create Docker container
        run: docker create --name mibox-ui-${{ matrix.pkg_type }} mibox-ui-${{ matrix.pkg_type }}:ci

      - name: Copy package from Docker container
        run: |
          mkdir -p ./pkg-out
          docker cp mibox-ui-${{ matrix.pkg_type }}:/builder/bin/packages/x86_64/luci/. ./bin-${{ matrix.pkg_type }}/
          if [ "${{ matrix.pkg_type }}" = "ipk" ]; then
            cp ./bin-${{ matrix.pkg_type }}/luci-app-mibox-ui*.ipk ./pkg-out/luci-app-mibox-ui.ipk
          else
            cp ./bin-${{ matrix.pkg_type }}/luci-app-mibox-ui*.apk ./pkg-out/luci-app-mibox-ui.apk
          fi

      - name: Remove Docker container
        run: docker rm mibox-ui-${{ matrix.pkg_type }}

      - name: Upload built package
        uses: actions/upload-artifact@v4.6.2
        with:
          name: pkg-${{ matrix.pkg_type }}
          path: pkg-out/

  # Джоб для создания Release
  release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Download ipk artifact
        uses: actions/download-artifact@v4
        with:
          name: pkg-ipk
          path: release-ipk/

      - name: Download apk artifact
        uses: actions/download-artifact@v4
        with:
          name: pkg-apk
          path: release-apk/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2.4.0
        with:
          files: |
            release-ipk/*.ipk
            release-apk/*.apk
